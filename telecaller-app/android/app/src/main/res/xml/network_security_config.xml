<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow self-signed certificate for production server -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">13.206.154.118</domain>
        <trust-anchors>
            <!-- Trust user-added CAs and system CAs -->
            <certificates src="user" />
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

    <!-- Development: Allow cleartext for localhost -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">192.168.0.0/16</domain>
    </domain-config>

    <!-- Base config: trust system CAs -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
